# ระบบติดตามและแจ้งเตือนราคาคริปโทเคอร์เรนซีอัตโนมัติ (Serverless Crypto Tracker)

**ผู้จัดทำ:** [นายธนกร ส่งไพศาล,นายมินธาดา เอนกลาภ,นายวัทธิกร ขวัญแดง]

แอปพลิเคชันแบบ Serverless 100% บน AWS ที่ทำหน้าที่ตรวจสอบราคา Cryptocurrency (Bitcoin, Ethereum, Dogecoin, Solana) จากตลาดโลกโดยอัตโนมัติ และทำการส่งแจ้งเตือนแบบ Real-time ผ่านทางอีเมล (SNS) เมื่อราคาลดลงมาถึงจุดเป้าหมายที่คุณตั้งไว้

ระบบนี้ถูกออกแบบมาให้ทำงานได้ด้วยต้นทุนที่ต่ำ (หรือฟรีใน AWS Free Tier), มีความเสถียรสูง และให้ความสำคัญกับความปลอดภัยของข้อมูลโดยการเข้ารหัส Database ด้วย KMS

-----

## 🏛️ สถาปัตยกรรมระบบ (Architecture)

แอปพลิเคชันนี้ใช้สถาปัตยกรรมแบบ Event-driven architecture ที่เรียบง่ายและทรงพลัง:

1.  **Amazon EventBridge (Scheduler):** ตัวตั้งเวลาแบบ Cron job (ทำงานทุก 1 ชั่วโมง) เพื่อไปกระตุ้นให้ Lambda เริ่มทำงาน
2.  **AWS Lambda (Compute):** ฟังก์ชันหลักที่เขียนด้วย Python 3.12 จะเริ่มประมวลผลเมื่อถูกเรียกใช้งาน
3.  **Amazon DynamoDB (Config & Storage):** Lambda จะไปอ่านค่าการตั้งค่า (เช่น รายชื่อเหรียญ, ราคาเป้าหมาย) จากฐานข้อมูลนี้ ซึ่งถูกเข้ารหัสความปลอดภัยไว้ด้วย **AWS KMS**
4.  **CoinGecko API (Data):** ฟังก์ชันจะดึงราคาตลาดล่าสุดของเหรียญทั้งหมดผ่าน Public API
5.  **Logic Processing:** ระบบจะเปรียบเทียบราคาปัจจุบันกับราคาเป้าหมาย (Target Price)
6.  **Amazon SNS (Alert):** หากพบว่าราคา "ต่ำกว่า" เป้าหมาย ระบบจะส่งข้อความไปยัง SNS Topic เพื่อส่งอีเมลแจ้งเตือนหาคุณทันที
7.  **DynamoDB Update:** ระบบจะบันทึกราคาล่าสุดที่เช็คได้ กลับลงไปในฐานข้อมูลเพื่อเก็บประวัติ (Last Checked Price)

-----

## ⚙️ เทคโนโลยีที่ใช้ (Technology Stack)

* **AWS Services:**
    * AWS Lambda (Python 3.12 Runtime)
    * Amazon DynamoDB (Database)
    * AWS KMS (Encryption/Security)
    * Amazon SNS (Notification)
    * Amazon EventBridge (Automation/Scheduler)
* **External Data:**
    * CoinGecko API (Free Tier)
* **Libraries:**
    * `boto3` (AWS SDK for Python)
    * `urllib3` (สำหรับการทำ HTTP Request โดยไม่ต้องลง Layer เพิ่มเติม)

-----

## 🚀 ขั้นตอนการติดตั้ง (Deployment Guide)

ทำตามขั้นตอนด้านล่างนี้เพื่อสร้างระบบบน AWS Account ของคุณ

### 1. การตั้งค่าฐานข้อมูล (DynamoDB Setup)

1.  ไปที่ **Amazon DynamoDB** > **Create table**.
2.  **Table name:** `PriceTrackingConfig`
3.  **Partition key:** `productName` (String).
4.  **Table settings:** เลื่อนไปที่ **Encryption at rest** เลือก `AWS managed key` (เพื่อเปิดใช้งาน KMS).
5.  คลิก **Create table**.
6.  **Add Items:** เพิ่มรายการเหรียญที่ต้องการ (เช่น Bitcoin, Ethereum, Dogecoin, Solana) โดยระบุ `targetPrice` และ `emailAddress`.

### 2. การตั้งค่าระบบแจ้งเตือน (SNS Setup)

1.  ไปที่ **Amazon SNS** > **Topics** > **Create topic**.
2.  **Type:** `Standard`.
3.  **Name:** `PriceAlertTopic`.
4.  หลังจากสร้างเสร็จ ให้ก๊อปปี้ **ARN** เก็บไว้ (เช่น `arn:aws:sns:us-east-1:123456789012:PriceAlertTopic`).
5.  คลิก **Create subscription** > Protocol: `Email` > Endpoint: `ใส่อีเมลของคุณ`.
6.  **สำคัญ:** ไปที่ Inbox อีเมลของคุณและกดลิงก์ **Confirm subscription**.

### 3. การสร้างฟังก์ชันประมวลผล (Lambda Setup)

1.  ไปที่ **AWS Lambda** > **Create function**.
2.  **Name:** `PriceCheckerFunction`
3.  **Runtime:** ‼️ **Python 3.12** ‼️
4.  **Execution role:** เลือก `Use an existing role` และเลือก `LabRole` (หรือ Role ที่มีสิทธิ์เข้าถึง DynamoDB และ SNS).
5.  **Code:**
    * นำโค้ด Python (จากไฟล์ `lambda_function.py`) ไปวางใน Code Editor.
    * **IMPORTANT:** แก้ไขตัวแปร `SNS_TOPIC_ARN` ในโค้ด ให้เป็น ARN ของคุณที่ก๊อปปี้มาจากขั้นตอนที่ 2.
    * คลิก **Deploy**.

### 4. การตั้งค่าระบบอัตโนมัติ (EventBridge Scheduler Setup)

1.  ไปที่หน้า **Lambda Function** ของคุณ
2.  คลิก **+ Add trigger**.
3.  เลือก source เป็น **EventBridge (CloudWatch Events)**.
4.  **Rule:** `Create a new rule`.
5.  **Rule name:** `RunPriceCheckerHourly`.
6.  **Schedule expression:** `rate(1 hour)`.
7.  คลิก **Add**.

-----

## 🧪 วิธีการทดสอบ (How to Test)

คุณไม่ต้องรอให้ครบ 1 ชั่วโมงเพื่อให้ระบบทำงาน คุณสามารถกดทดสอบได้ทันที:

1.  ไปที่ฟังก์ชัน Lambda `PriceCheckerFunction` ของคุณ.
2.  เลือกแท็บ **Test**.
3.  สร้าง Test event ใหม่ (ตั้งชื่อว่า `Test1` แล้วกด Save).
4.  คลิกปุ่ม **Test**.
5.  **ผลลัพธ์ที่ควรได้:**
    * **Logs:** แสดงสถานะ "Succeeded" และเห็นราคาปัจจุบันของเหรียญใน Log.
    * **Email:** หากราคาต่ำกว่าเป้าหมาย คุณจะได้รับอีเมลแจ้งเตือนทันที.
    * **DynamoDB:** ค่า `lastCheckedPrice` ในตารางจะถูกอัปเดตเป็นราคาปัจจุบัน.

-----
*Created for AWS Project Submission*
